import {Role} from './role';

export class User {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  password: string;
  id: number;
  login: string;
  role: Role;
  createdAt: string;
  updatedAt: string;
  deletedAt: string;
  address: string;
  birthday: string;
  isActive: boolean;
}
